description: INFINEON AIROC GPIO PORT node

compatible: "infineon,airoc-gpio"

include: [gpio-controller.yaml, base.yaml]

properties:
  reg:
    required: false

  "#gpio-cells":
    const: 2

gpio-cells:
  - pin
  - flags
